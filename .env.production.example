# Пример переменных окружения для продакшена
# Скопируйте этот файл в .env.production.local и заполните значения

# URL бэкенда (используется в rewrites)
NEXT_PUBLIC_API_URL=https://bank.korzik.space/api

# Для Docker - внутренний адрес бэкенда
BACKEND_HOST=backend
BACKEND_PORT=8080
